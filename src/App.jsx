import DynamicForm from "./components/DynamicForm";
import { ToastContainer } from "react-toastify";

const App = () => (
  <div>
    <DynamicForm />
    <ToastContainer />
  </div>
);

export default App;
